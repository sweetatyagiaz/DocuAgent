# These versions are pinned to exactly what was tested together end-to-end
# in this project (Phases 2-8) to avoid dependency-resolution surprises.

# --- Core web/backend ---
fastapi==0.141.1
uvicorn[standard]==0.52.4
python-dotenv==1.2.2
pydantic==2.13.4

# --- LLM + Agent framework ---
anthropic==0.125.0
langchain==1.3.17
langchain-anthropic==1.6.1
langchain-core==1.6.0
langgraph==1.2.11

# --- RAG / embeddings / vector store ---
chromadb==1.5.9
numpy==2.4.4
pypdf==5.9.0
python-docx==1.2.0
# sentence-transformers==3.1.1   # optional upgrade: swap in for higher-quality embeddings (see app/rag/embeddings.py)
# unstructured==0.15.13           # optional: richer document parsing

# --- Tools ---
tavily-python==0.8.0

# --- Frontend ---
streamlit==1.62.0

# --- Testing ---
pytest==9.1.1
